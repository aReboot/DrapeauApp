<resources>

    <!-- Base application theme. -->
    <style name="MyTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowNoTitle">true</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/windowsBackground</item>
        <item name="android:textColorPrimary">@color/windowsBackground</item>
        <item name="android:buttonStyle">@color/androidButton</item>
        <item name="android:progressBarStyleHorizontal">@color/spinner</item>


    </style>



</resources>
